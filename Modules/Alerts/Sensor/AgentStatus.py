#!/usr/bin/env pythonimport sysimport os, time, pdbimport Sensorclass AgentStatus (Sensor.Connection):    def __init__():        self.sensor = Sensor.Connection()    def getAgentStatus (self):            sensor.cursor.execute ("""select node,agent,state,time_update from t_agent_status""")        table = self.sensor.cursor.fetchall()                for row in table:            agent [0] = row[0]            agent [1] = row[1]            agn_result [0] = row[2]            agn_result [1] = row[3]                              table_agent_status [agent] = agn_result                    return table_agent_status                    